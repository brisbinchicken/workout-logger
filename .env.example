# Security
FLASK_SECRET_KEY=change_this_to_a_random_string
ADMIN_USERNAME=admin
# Generate hash using bcrypt (or the app will auto-generate one on first run if empty)
ADMIN_PASSWORD_HASH=

# Configuration
TIMEZONE=UTC
LAN_PORT=8080

# Paths (Docker)
SMB_MOUNT_HOST_PATH=./mnt/workouts
EXPORTS_SUBDIR=exports
BACKUPS_SUBDIR=backups
